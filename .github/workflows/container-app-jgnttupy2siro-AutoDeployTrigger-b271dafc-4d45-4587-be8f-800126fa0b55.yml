name: Trigger auto deployment for container-app-jgnttupy2siro

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-jgnttupy2siro-AutoDeployTrigger-b271dafc-4d45-4587-be8f-800126fa0b55.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPJGNTTUPY2SIRO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPJGNTTUPY2SIRO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPJGNTTUPY2SIRO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPJGNTTUPY2SIRO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPJGNTTUPY2SIRO_REGISTRY_PASSWORD }}
          containerAppName: container-app-jgnttupy2siro
          resourceGroup: todonodejspgsqlgoogleauthbef88586
          imageToBuild: default/container-app-jgnttupy2siro:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


